# Core CLI framework
typer>=0.9.0

# AWS SDK
boto3>=1.34.0

# UI and formatting
rich<14.3.0

# HTTP client
requests>=2.34.2

# Caching
diskcache>=5.6.0

# Data handling
python-dateutil>=2.8.0
PyYAML>=6.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0

# Optional: System monitoring (used in health checks)
psutil>=5.9.0

# Web dashboard
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
# Pin pydantic + pydantic-core together. CI's pip cache has bitten us when
# pydantic floated: the resolver picked a newer pydantic whose required
# pydantic-core didn't match the cached wheel on disk, and PyInstaller
# bundled the mismatched pair — runtime SystemError on binary launch.
pydantic==2.12.5
pydantic-core==2.41.5
aiofiles>=23.0.0

# Rate limiting
slowapi>=0.1.9
